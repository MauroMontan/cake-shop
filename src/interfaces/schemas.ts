export interface Cake {
  cakeName: string;
  cover: string;
  customMessage: string;
  price: number;
  messageColor: string;
  cakeDescription: string;
  freeLactose: boolean;
  cakePhoto: string;
}

export interface User {
  email: string;
  password: string;
}
